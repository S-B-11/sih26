# 4.4 Risk Assessment Agent

- `risk_agent.py` — combines wave height, wind, cyclone proximity and
  lightning risk into a transparent weighted safety score.

Keep this an explicit, readable formula rather than a learned model —
explainability is a scored requirement for this problem statement.
