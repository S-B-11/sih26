# 5. Synthesis / Aggregator Agent

**Flowchart layer 5 — combines every agent's output into one
evidence-cited, explainable answer.**

- `synthesis_agent.py` — merges agent outputs, produces the answer text,
  confidence score and evidence list.
